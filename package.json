{
  "name": "ganan",
  "version": "1.0.0",
  "private": true,
  "description": "Regression tests for Ganan group expense splitter",
  "scripts": {
    "test": "npm run test:regression",
    "test:regression": "playwright test",
    "test:regression:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "http-server": "^14.1.1"
  }
}
